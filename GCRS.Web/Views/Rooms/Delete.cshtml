@model GCRS.Base.Room

@{
    ViewBag.Title = "Delete";
}

<h2>Eliminar</h2>

<h3>¿Está seguro de que desea elminar esta Habitación?</h3>
<div>
    <h4>Habitación</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RoomType.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RoomType.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.SquareMts)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SquareMts)
        </dd>
    </dl>

    @{
        ViewDataDictionary viewData = new ViewDataDictionary();
        viewData.Add(new KeyValuePair<string, object>("details", true));
    }

    <label style="font-size: 16px; font-weight: 700">Características</label>
    <div class="one-to-many-table-container my-form-container4">
        @Html.Partial("RoomFeaturesTablePartial", Model.Features.ToList(), viewData)
    </div>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Eliminar" class="btn btn-default" /> |
            @Html.ActionLink("Regresar", "Index")
        </div>
    }
</div>
